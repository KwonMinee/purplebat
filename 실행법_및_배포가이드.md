# 보라박쥐단 홈페이지 — 풀스택(백엔드 포함) 버전 실행법 & 배포 가이드

먼저 중요한 사실 하나를 짚고 갈게요. **진짜 백엔드(로그인, 관리자 인증, 데이터 저장)는 서버 프로그램이 켜져 있어야만 동작합니다.** HTML 파일을 그냥 더블클릭해서 여는 것만으로는 로그인도, 방명록 저장도 되지 않아요. 이건 기술적으로 당연한 제약이라 저도 우회할 수 없었습니다 — 다만 **서버를 켜는 과정을 "파일 하나 더블클릭"에 최대한 가깝게** `start.bat`(윈도우) / `start.sh`(맥·리눅스) 실행 스크립트로 만들어 뒀습니다. 더블클릭 한 번이면 서버가 켜지고 브라우저까지 자동으로 열립니다.

## 0. ⚠️ 중요 업데이트: 데이터베이스가 MongoDB로 바뀌었습니다

예전에는 관리자 모드에서 수정한 내용이 `data/*.json` 파일에 저장됐는데, Render 같은 호스팅은 재배포/재시작할 때마다 디스크를 초기화해서 **수정 내용이 자꾸 사라지는 문제**가 있었습니다. 이제는 무료 클라우드 DB인 **MongoDB Atlas**에 저장하도록 바꿨기 때문에, 재배포/재시작해도 데이터가 그대로 유지됩니다.

**이 버전을 실행하려면 아래 "8. MongoDB Atlas 설정"을 먼저 따라 해서 `MONGODB_URI` 값을 준비해야 합니다.** 이 값이 없으면 서버는 켜지지만 로그인/게시판 등 데이터 관련 기능이 전부 오류를 냅니다.

## 1. 무엇이 추가됐나요

- **블로그형 글쓰기 에디터** (`js/rich-editor.js` 공용 모듈): 굵게/기울임/밑줄, 글꼴, 글자 크기, 글자 색, 정렬, 목록, 인용구, 구분선 같은 서식은 물론, 사진(파일 선택 시 자동으로 용량을 줄여 삽입) · 링크 · 유튜브/비메오 영상 삽입까지 지원하는 리치 텍스트 에디터입니다. 아래 두 곳에서 동일하게 사용됩니다.
  - **게시판 글쓰기** (`admin-write.html`): 관리자 전용 글쓰기 화면. 작성한 글은 `post.html?id=...`라는 고유 주소로 발행되어 로그인하지 않은 외부인도 링크로 바로 볼 수 있고, "공유하기" 버튼으로 링크를 복사할 수 있습니다. 수정·삭제 버튼은 관리자로 로그인했을 때만 보입니다.
  - **캐릭터 방 "새 글 작성"** (`character.html?id=...`): 관리자 도구 안에 같은 에디터가 그대로 들어가 있어 캐릭터 이야기 게시글도 사진/영상/링크를 넣어 작성할 수 있습니다. 목록에서 글을 열면 나오는 "수정" 버튼을 누르면 이 에디터에 기존 내용이 채워져 바로 고칠 수 있습니다.
  - **아카이브 "새 기록 작성"** (`archive.html`): 관리자 도구 안에 같은 에디터가 들어가 있어 사진/영상 후기 등을 블로그처럼 작성할 수 있습니다. 카드를 클릭하면 상세 내용이 모달로 열리고, 관리자에게는 수정/삭제 버튼이 함께 보입니다. 대표 이미지를 직접 입력하지 않으면 본문의 첫 사진이 자동으로 카드 썸네일이 됩니다.
  - **미니게임 "새 게임 등록"** (`minigame.html`): 관리자 도구 안에 같은 에디터가 들어가 있어 게임 소개·조작법·스크린샷·플레이 영상 등을 자유롭게 작성할 수 있습니다. 난이도, 아이콘 이모지, 뱃지 문구, 카드 그라디언트 색상도 함께 설정합니다. 카드를 클릭하면 상세 모달이 열리고(게임 시작 버튼과는 별개), 관리자에게는 수정/삭제 버튼이 보입니다.
  - **굿즈샵 "새 상품 등록"** (`goods.html`): 관리자 도구 안에 같은 에디터가 들어가 있어 상품 소개, 구성품, 사이즈 안내를 사진/영상과 함께 작성할 수 있습니다. 상품명·가격·카테고리와 함께 설정하고, 대표 이미지를 비워두면 본문 첫 사진이 자동으로 카드 썸네일이 됩니다. 카드를 클릭하면 상세 모달이 열리고(장바구니 담기 버튼은 그대로 동작), 관리자에게는 수정/삭제 버튼이 보입니다.
- **🧱 콕슨의 벽돌깨기** (`brick-breaker.html`): 실제로 플레이할 수 있는 벽돌깨기 미니게임입니다. 벽돌을 깰 때마다 뒤에 숨어있는 콕슨의 이미지가 조금씩 드러나고, 다 깨면 전체 모습이 공개됩니다. 마우스/터치로 패들을 움직이고 클릭(탭)이나 스페이스바로 공을 발사합니다. 외부 라이브러리 없이 순수 Canvas로 만들었고(`js/brick-breaker.js`), `minigame.html` 상단의 보라색 배너 카드에서 바로 들어갈 수 있습니다. 이 게임은 관리자가 등록한 미니게임 목록(DB)과는 별개로 항상 고정 노출되는 페이지입니다.
- **백엔드 서버** (Node.js + Express): 공지사항, 아카이브, 굿즈, 방명록, 게시판 데이터를 서버에 저장하고 불러옵니다. 데이터는 MongoDB Atlas(무료 클라우드 DB)에 저장되어 재배포/재시작해도 사라지지 않습니다.
- **관리자 인증**: `admin-login.html`에서 로그인/회원가입을 해야만 관리자 대시보드(`admin.html`)에 들어갈 수 있습니다.
  - 관리자 계정이 하나도 없을 때만 회원가입이 가능합니다 (아무나 관리자 계정을 만들 수 없도록 하는 안전장치).
  - 비밀번호는 bcrypt로 안전하게 암호화되어 저장됩니다.
  - 로그인 세션은 8시간 동안 유지됩니다.
- **관리자 대시보드**: 공지사항 추가/삭제, 방명록 모더레이션(삭제), 전체 현황 통계를 볼 수 있습니다. 게시판/캐릭터 방/아카이브/미니게임/굿즈샵처럼 블로그형 에디터가 필요한 항목은 대시보드가 아니라 해당 페이지에서 직접 작성·수정·삭제합니다(대시보드에는 목록 확인, 삭제 버튼, "작성하러 가기" 버튼만 있습니다).
- **프론트엔드는 그대로 작동**: 서버를 켜지 않고 `public/index.html`을 직접 열어도 기존처럼 정적인 예시 데이터로 화면이 보입니다(로그인/저장 기능 제외). 서버를 켜면 실시간 데이터로 자동 전환됩니다.

## 2. 파일 구조

```
borabakjwidan-fullstack/
├── start.bat                 # Windows 실행 스크립트 (더블클릭)
├── start.sh                  # macOS/Linux 실행 스크립트
├── server.js                 # 백엔드 서버 진입점
├── package.json
├── download-assets.ps1 / .sh # 이미지 다운로드 스크립트 (public/assets에 저장)
├── routes/
│   ├── auth.routes.js        # 회원가입/로그인/로그아웃
│   ├── public.routes.js      # 공개 조회 API + 방명록 작성
│   └── admin.routes.js       # 관리자 전용 CRUD API (인증 필요)
├── middleware/requireAdmin.js
├── lib/jsonStore.js          # MongoDB 저장소 (이름은 예전 그대로지만 내부는 MongoDB 연동)
├── .env.example               # MONGODB_URI 등 환경변수 예시 (복사해서 .env로 사용)
├── data/                     # 최초 1회 DB가 비었을 때 자동으로 넣어주는 시드 데이터
└── public/                   # 프론트엔드 정적 파일 (서버가 이 폴더를 그대로 서빙)
    ├── index.html, party.html, archive.html, minigame.html, goods.html, community.html
    ├── admin-login.html, admin.html
    ├── css/style.css, css/admin.css
    ├── js/api.js, js/script.js, js/admin.js
    └── assets/                # 이미지 (다운로드 스크립트 실행 필요)
```

## 3. 실행 방법 (최초 1회 + 매번 사용할 때)

> 시작하기 전에 "8. MongoDB Atlas 설정"을 먼저 완료해서 `.env` 파일에 `MONGODB_URI`를 넣어두세요.

### Windows
1. 압축을 풀고 `borabakjwidan-fullstack` 폴더로 들어갑니다.
2. **`start.bat`을 더블클릭**합니다.
   - 처음 실행할 때는 자동으로 `npm install`이 실행되며 1~2분 정도 걸립니다 (인터넷 연결 필요, Node.js가 미리 설치되어 있어야 해요 → https://nodejs.org 에서 LTS 버전 설치).
   - 설치가 끝나면 서버가 켜지고, 2초 뒤 브라우저가 자동으로 `http://localhost:3000`을 엽니다.
3. 다음부터는 `start.bat`을 더블클릭하기만 하면 바로 서버가 켜집니다 (재설치 없음).
4. 종료하려면 열려있는 검은 콘솔 창을 닫으면 됩니다.

### macOS / Linux
```bash
chmod +x start.sh
./start.sh
```
동일하게 최초 1회는 `npm install`이 자동 실행되고, 이후에는 바로 서버가 켜집니다.

### 이미지 받기 (최초 1회, 선택이지만 권장)
```powershell
# Windows
powershell -ExecutionPolicy Bypass -File .\download-assets.ps1
```
```bash
# macOS/Linux
chmod +x download-assets.sh && ./download-assets.sh
```
Figma 임시 링크는 약 7일 후 만료됩니다. 만료 시 Figma에서 다시 내보내 `public/assets`에 같은 이름으로 넣어주세요. 이미지가 없어도 사이트는 정상적으로 열리고, 자리에 패턴 placeholder가 보입니다.

## 4. 관리자 모드 사용법

1. 아무 페이지 하단 푸터의 **"관리자"** 링크를 클릭하거나, 브라우저에서 `http://localhost:3000/admin-login.html`로 접속합니다.
2. **최초 1회**: "회원가입" 탭에서 원하는 아이디(3자 이상)와 비밀번호(6자 이상)로 관리자 계정을 만듭니다. 계정은 이 순간부터 하나만 존재하며, 이후에는 회원가입 탭이 비활성화되고 로그인만 가능합니다.
3. 로그인하면 관리자 대시보드로 이동합니다. 여기서:
   - 공지사항 항목을 추가하거나 삭제할 수 있습니다.
   - 방문자가 남긴 방명록을 확인하고, 부적절한 내용은 삭제할 수 있습니다.
   - 게시판 · 아카이브 · 미니게임 · 굿즈샵은 각 페이지(`community.html`의 글쓰기 화면, `archive.html`, `minigame.html`, `goods.html`)에 있는 블로그형 에디터로 이동해서 작성/수정하고, 대시보드에서는 목록 확인과 삭제만 합니다.
   - 전체 현황(공지 수, 아카이브 수 등)을 한눈에 볼 수 있습니다.
4. 로그아웃은 대시보드 우측 상단의 "로그아웃" 버튼을 누르면 됩니다.
5. 관리자가 추가한 내용은 즉시 홈페이지(공지사항, 아카이브, 굿즈샵, 미니게임 등)에 반영됩니다 — 새로고침하면 바로 보여요.

> 비밀번호를 잊어버렸다면: MongoDB Atlas 사이트에 로그인 → 클러스터의 "Browse Collections" → `users` 컬렉션에서 기존 계정 문서를 삭제하면, 다음 접속 시 회원가입을 다시 할 수 있습니다. (로컬의 `data/users.json`을 수정하는 것은 이제 아무 효과가 없습니다 — 그 파일은 DB가 완전히 비어있을 때 딱 한 번만 쓰이는 시드 데이터입니다.)

## 5. 브라우저(인터넷)에 배포하는 방법

⚠️ 이전 버전(정적 사이트)과 달리, 이제는 **Node.js 서버가 항상 켜져 있어야 하는 사이트**입니다. 그래서 Netlify Drop이나 GitHub Pages 같은 "정적 파일 호스팅"에는 그대로 올릴 수 없어요 (백엔드 API가 동작하지 않습니다). 아래처럼 **Node.js를 실행할 수 있는 호스팅**을 사용해야 합니다.

### 방법 A. Render.com (추천 — 무료 티어 있음)
1. https://render.com 가입 후 "New +" → "Web Service"
2. GitHub 저장소를 연결하거나 파일을 업로드
3. 설정:
   - Build Command: `npm install`
   - Start Command: `node server.js`
   - Environment Variable 2개 추가:
     - `MONGODB_URI` = "8. MongoDB Atlas 설정"에서 만든 연결 문자열 (⚠️ 이게 없으면 데이터 관련 기능이 전부 동작하지 않습니다)
     - `SESSION_SECRET` = 임의의 긴 문자열 (보안을 위해 꼭 설정 권장)
4. 배포되면 `https://프로젝트이름.onrender.com` 주소가 생성됩니다.

### 방법 B. Railway.app
1. https://railway.app 가입 후 새 프로젝트 생성, GitHub 저장소 연결
2. Node.js 프로젝트로 자동 인식되며, Start Command는 `node server.js`
3. `MONGODB_URI`, `SESSION_SECRET` 환경변수를 설정하고 배포

### 방법 C. 자체 서버(VPS)
Node.js가 설치된 서버(AWS EC2, Cafe24, 가비아 등)에 파일을 올린 뒤:
```bash
npm install
npm install -g pm2
MONGODB_URI="..." pm2 start server.js --name borabakjwidan
```
`pm2`를 사용하면 서버가 꺼져도 자동으로 재시작되고, 컴퓨터를 재부팅해도 계속 실행되도록 설정할 수 있습니다 (`pm2 startup`, `pm2 save`).

어떤 방법을 쓰든 배포 후에는 `MONGODB_URI`와 `SESSION_SECRET` 환경변수를 꼭 설정해주세요. 도메인은 이후에 원하는 곳에서 구매해 연결하면 됩니다.

## 6. 알아두면 좋은 제한사항

- **로그인 세션 저장소**: 현재는 서버 메모리에 세션을 저장합니다(`express-session` 기본 MemoryStore). 서버를 재시작하면 로그인이 풀립니다(다시 로그인하면 됨). 게시글/캐릭터 데이터 자체는 MongoDB에 저장되므로 서버 재시작과 무관하게 유지됩니다.
- **이미지 업로드**: 관리자 대시보드에서 이미지는 파일 업로드가 아니라 `public/assets/` 폴더 안의 파일명을 직접 입력하는 방식입니다. 새 이미지를 쓰려면 해당 파일을 `public/assets/`에 먼저 넣고 git으로 커밋·푸시해야 배포 사이트에도 반영됩니다.
- **MongoDB Atlas 무료 티어(M0)**: 512MB까지 무료로 영구 사용 가능합니다(이 프로젝트 규모에는 충분). 카드 등록 없이 가입 가능합니다.
- **검증 관련 안내**: 이 코드는 작업 환경의 네트워크 제한 때문에 실제로 `npm install` 및 MongoDB 연결을 실행하는 종단 간(End-to-End) 테스트는 진행하지 못했습니다. 대신 모든 JavaScript 파일 문법 검사(`node --check`), 모든 JSON 데이터 유효성 검사, 페이지 간 링크·경로 무결성 검사는 통과했습니다. 처음 실행 시 오류가 발생하면 오류 메시지를 알려주시면 함께 확인하겠습니다.

## 7. 다음에 더 손볼 만한 부분

- 회원가입/로그인을 관리자뿐 아니라 일반 방문자에게도 열어주는 기능(현재는 방명록/게시판이 로그인 없이도 누구나 작성 가능한 구조입니다)
- 이미지 업로드 기능(현재는 파일명을 직접 입력)
- 로그인 세션도 MongoDB에 저장(`connect-mongo` 패키지)해서 서버 재시작 후에도 로그인이 유지되게 하기

## 8. MongoDB Atlas 설정 (최초 1회 필수)

무료 클라우드 데이터베이스를 만드는 과정입니다. 5~10분 정도 걸립니다.

1. https://www.mongodb.com/cloud/atlas/register 에서 무료 회원가입 (Google 계정으로도 가능, 카드 등록 불필요)
2. 가입 후 나오는 화면에서 "Build a Database" 클릭 → **M0 (Free)** 플랜 선택 → 리전은 가까운 곳(예: Seoul, Tokyo) 선택 → "Create"
3. **Security Quickstart** 화면에서:
   - "Username and Password" 방식으로 DB 접속 계정 생성 (아이디/비밀번호를 꼭 메모해두세요 — 이건 사이트 관리자 계정과는 별개입니다)
   - "My Local Environment" 선택 → IP Access List에서 **"Add a Different IP Address"에 `0.0.0.0/0` 입력**(어디서나 접속 허용 — Render 같은 호스팅 서버는 IP가 매번 바뀌기 때문에 필요합니다) → "Add Entry" → "Finish and Close"
4. 왼쪽 메뉴 "Database"에서 방금 만든 클러스터의 **"Connect"** 버튼 클릭 → "Drivers" 선택 → Node.js 선택
5. 표시되는 연결 문자열을 복사합니다. 형태는 이렇습니다:
   ```
   mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
6. `<username>`, `<password>`를 3번에서 만든 실제 아이디/비밀번호로 바꾸고, `mongodb.net/` 뒤에 DB 이름을 추가해주세요 (예: `mongodb.net/borabakjwidan?retryWrites=...`). 비밀번호에 `@`, `#`, `/` 같은 특수문자가 있으면 오류가 날 수 있으니, 이런 문자가 없는 비밀번호를 쓰는 걸 추천합니다.
7. 완성된 연결 문자열을 아래처럼 사용합니다:
   - **로컬에서 실행할 때**: `.env.example` 파일을 복사해서 같은 폴더에 `.env`로 저장 → `MONGODB_URI=` 뒤에 붙여넣기
   - **Render 등에 배포할 때**: 대시보드의 Environment 설정에서 Key `MONGODB_URI`, Value에 연결 문자열을 붙여넣기
8. 서버를 처음 실행(또는 재배포)하면, DB가 비어있는 걸 감지해서 `data/*.json`에 있던 시드 데이터를 자동으로 한 번 넣어줍니다. 캐릭터 5명(및 캐릭터 게시글)만 예시로 들어가고, 공지사항/아카이브/굿즈/방명록/게시판/미니게임은 빈 상태로 시작해서 관리자 모드에서 직접 채워나가는 구조입니다. 그 다음부터는 관리자 모드에서 수정한 내용이 계속 MongoDB에 쌓이고, 재배포해도 사라지지 않습니다.

## 9. 예시(더미) 게시글 정리하기

이전 버전에는 공지사항/아카이브/굿즈/방명록/게시판/미니게임에 데모용 예시 글이 미리 들어가 있었습니다. 이미 MongoDB에 그 예시 글이 저장되어 있는 상태라면(관리자 대시보드에 낯선 글들이 보인다면), 아래 명령어로 한 번에 정리할 수 있습니다.

```bash
npm run reset-example-data
```

이 명령은 `notices`, `archive`, `goods`, `guestbook`, `board`, `minigame` 컬렉션의 내용을 전부 지웁니다. **캐릭터 정보와 캐릭터 게시판 글, 관리자 계정은 그대로 유지됩니다.** 로컬에서 `.env`에 `MONGODB_URI`가 설정되어 있어야 실행됩니다(= 배포 서버가 아니라 내 컴퓨터에서 한 번만 실행하면, 배포 서버가 보는 것과 같은 DB이므로 사이트에도 바로 반영됩니다). 실행 후에는 관리자 대시보드에서 새로 작성하는 글만 남습니다.

## 10. 캐릭터 이름 변경하기 (아르곤→로완, 제드릭→루스, 엘리아→콕슨, 리온→굴리아나)

파티원 페이지, 홈, 아카이브 등 화면에 보이는 글자는 이미 새 이름으로 바뀌어 있습니다. 하지만 서버(MongoDB)에 예전 이름으로 이미 저장되어 있던 캐릭터 데이터는 화면 코드만 고친다고 자동으로 바뀌지 않으므로, 아래 명령어를 **한 번만** 실행해서 정리해주세요.

```bash
npm run rename-characters
```

이 명령은 `characters` 컬렉션에서 아르곤/제드릭/엘리아/리온의 `name` 필드를 각각 로완/루스/콕슨/굴리아나로 바꾸고, `characterPosts`(캐릭터 방 게시글)의 제목·본문에 옛 이름이 들어있으면 새 이름으로 바꿔줍니다. 마찬가지로 로컬 `.env`에 `MONGODB_URI`가 설정되어 있어야 하며, 한 번 실행하면 배포된 사이트에도 바로 반영됩니다. 이미 이 명령을 실행했다면 다시 실행해도 안전합니다(이미 바뀐 이름은 그대로 유지됩니다).
